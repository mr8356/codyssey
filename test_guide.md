### 💡 확인 가이드: 성공 및 실패 케이스 콘솔 출력 테스트

#### 1단계: 정상 상태 확인 (모두 PASS)
먼저 코드가 정상적인 데이터를 만났을 때 어떻게 출력되는지 확인합니다.

1. 터미널(명령 프롬프트)에서 `python main.py`를 실행합니다.
2. 메뉴에서 **`2`** (JSON 데이터 분석 모드)를 입력하고 엔터를 칩니다.
3. 맨 아래 콘솔 출력 결과를 확인합니다.
   ```text
   --- 결과 요약 ---
   전체 테스트: 4 | 통과: 4 | 실패: 0
   ```
   *(현재 제가 드린 `data.json`은 완벽한 정답셋이므로 실패가 0건으로 나옵니다.)*

#### 2단계: 의도적으로 에러 만들기 (FAIL 테스트)
실패 사유가 잘 출력되는지 확인하려면 `data.json` 파일을 잠시 수정해서 고의로 에러를 내보아야 합니다. `data.json` 파일을 열고 맨 아래 `patterns` 부분을 다음과 같이 살짝 수정해 보세요.

**테스트 A: 정답(Label) 틀리게 만들기**
* `size_5_01`의 `"expected": "+"` 부분을 `"expected": "x"` 로 바꿉니다. (로직은 맞지만 정답과 달라 실패함)

**테스트 B: 스키마(크기) 망가뜨리기**
* `size_5_02`의 `"input"` 배열에서 맨 마지막 줄 `[1,0,0,0,1]`을 통째로 지워버립니다. (5x5가 아니라 5x4가 되어 크기 검증에서 실패함)

파일을 저장합니다.

#### 3단계: 실패 케이스 콘솔 출력 확인
다시 터미널에서 `python main.py`를 실행하고 **`2번`** 모드를 선택합니다. 맨 아래 출력 결과를 확인해 보세요.

**[예상되는 콘솔 출력 화면]**
```text
--- 성능 분석 ---
크기 (NxN)      | 평균 연산 시간 (ms)  | 연산 횟수 (N²)  
-------------------------------------------------------
5x5             | 0.003500             | 25             
13x13           | 0.021100             | 169            
25x25           | 0.078500             | 625            

--- 결과 요약 ---
전체 테스트: 4 | 통과: 2 | 실패: 2
실패 케이스 목록:
 - size_5_01: 판정 실패 (예상: X, 실제: Cross)
 - size_5_02: 패턴 크기 불일치 (예상: 5x5)
```

**결과 검증 포인트:**
* 요구사항대로 **"전체 테스트 수 / 통과 수 / 실패 수"**가 정확히 요약되어 출력됩니다.
* 실패 케이스가 존재할 경우, **"케이스 식별자(`size_5_01` 등)"**와 그에 맞는 **"실패 사유(판정 실패, 크기 불일치 등)"**가 명확하게 분리되어 출력되는 것을 확인할 수 있습니다.

확인이 끝나셨다면, 과제 제출 전에는 다시 `data.json`을 원래대로 돌려놓으시면 됩니다!